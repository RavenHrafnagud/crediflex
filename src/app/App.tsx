import { PromotionsPage } from '../promociones/ui/PromotionsPage'

// Root app component delegating all business UI to the promotions domain.
const App = () => {
  return <PromotionsPage />
}

export default App
